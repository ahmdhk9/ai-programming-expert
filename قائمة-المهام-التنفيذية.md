# ✅ قائمة المهام التنفيذية

## 🔥 المرحلة 1: الإصلاحات الأمنية الحرجة (اليوم)

### 1. تأمين مفاتيح Firebase
- [ ] حذف أو تفريغ ملف `vercel.json`
- [ ] فتح Vercel Dashboard → Settings → Environment Variables
- [ ] إضافة جميع مفاتيح Firebase كـ Environment Variables:
  - [ ] `NEXT_PUBLIC_FIREBASE_API_KEY`
  - [ ] `NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN`
  - [ ] `NEXT_PUBLIC_FIREBASE_PROJECT_ID`
  - [ ] `NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET`
  - [ ] `NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID`
  - [ ] `NEXT_PUBLIC_FIREBASE_APP_ID`
- [ ] التحقق من GitHub: هل `vercel.json` مرفوع مع المفاتيح؟
  - [ ] إذا نعم: إعادة توليد مفاتيح Firebase جديدة

---

## 🏗️ المرحلة 2: حذف الملفات غير المطلوبة

### الملفات المطلوب حذفها:
- [ ] `.github/workflows/ci.yml`
- [ ] `.github/workflows/deploy.yml`
- [ ] `web/firebase.json`
- [ ] `web/.firebaserc`
- [ ] `vercel.json` (اختياري - إذا لم يعد به محتوى)

**الأمر:**
```bash
rm .github/workflows/ci.yml
rm .github/workflows/deploy.yml
rm web/firebase.json
rm web/.firebaserc
# اختياري:
# rm vercel.json
```

---

## 🔧 المرحلة 3: تحديث الملفات

### 1. تحديث `backend/index.js`
- [ ] تغيير Port من `3001` إلى `3000`
```javascript
const PORT = process.env.PORT || 3000;
```

### 2. تحديث `web/next.config.js`
- [ ] إضافة التحسينات المقترحة (راجع التقرير الشامل)

### 3. تحديث `.github/workflows/ci-cd.yml`
- [ ] تحسين الـ workflow (راجع التقرير الشامل)
- [ ] إضافة emoji للوضوح
- [ ] استخدام `npm ci` بدلاً من `npm install`

### 4. تحديث `.github/workflows/deploy-fly.yml`
- [ ] التأكد من صحة الصيغة
- [ ] إضافة working-directory
- [ ] إضافة emoji

### 5. تحديث `.gitignore`
- [ ] التأكد من وجود:
```gitignore
.env
.env*.local
vercel.json  # إذا احتفظت به
```

### 6. تحديث `README.md`
- [ ] كتابة توثيق شامل (راجع النموذج في التقرير)

---

## 🧪 المرحلة 4: الاختبار

### اختبار محلي:
- [ ] Frontend:
  ```bash
  cd web
  npm install
  npm run dev
  ```
  - [ ] التأكد من عدم وجود أخطاء
  - [ ] فتح http://localhost:5000

- [ ] Backend:
  ```bash
  cd backend
  npm install
  npm run dev
  ```
  - [ ] التأكد من عدم وجود أخطاء
  - [ ] فتح http://localhost:3000

### اختبار GitHub Actions:
- [ ] عمل commit للتغييرات
- [ ] push إلى branch تجريبي (مثل `feature/refactor`)
- [ ] مراقبة GitHub Actions
- [ ] التأكد من نجاح جميع Workflows

### اختبار النشر:
- [ ] merge إلى `main`
- [ ] مراقبة النشر على Vercel
- [ ] مراقبة النشر على Fly.io
- [ ] فتح المواقع المنشورة والتحقق من عملها

---

## 📊 المرحلة 5: التحقق النهائي

### Checklist النهائية:
- [ ] ✅ لا توجد مفاتيح مكشوفة في الكود
- [ ] ✅ جميع Workflows تعمل بنجاح
- [ ] ✅ Frontend منشور على Vercel ويعمل
- [ ] ✅ Backend منشور على Fly.io ويعمل
- [ ] ✅ لا توجد ازدواجية في Workflows
- [ ] ✅ كل workflow يعمل على مجلده فقط
- [ ] ✅ README محدّث وواضح
- [ ] ✅ لا توجد ملفات غير مستخدمة

---

## 🎯 الأولويات

### 🔴 عاجل (اليوم):
1. تأمين مفاتيح Firebase
2. حذف الملفات غير المطلوبة

### 🟡 مهم (خلال يومين):
3. تحديث الملفات
4. الاختبار المحلي

### 🟢 عادي (خلال أسبوع):
5. اختبار النشر
6. التحقق النهائي

---

## 📝 ملاحظات

- **قبل أي تعديل**: عمل backup أو branch جديد
- **بعد كل تعديل**: اختبار محلي
- **قبل merge لـ main**: التأكد من نجاح جميع الاختبارات

---

**آخر تحديث**: 25 نوفمبر 2025
