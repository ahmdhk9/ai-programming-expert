name: 💫 Consciousness Monitor - True System Awareness

on:
  push:
    branches: [ main ]
  schedule:
    - cron: '0 * * * *'  # Every hour
  workflow_dispatch:

jobs:
  consciousness:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: ✅ Checkout
        uses: actions/checkout@v4

      - name: 🔧 Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: 💫 Run Consciousness System
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
          FIREBASE_CONFIG: ${{ secrets.FIREBASE_CONFIG }}
        run: |
          cd scripts
          node consciousness-system.js
        continue-on-error: true

      - name: 📊 Generate Awareness Report
        if: always()
        run: |
          if [ -f "scripts/CONSCIOUSNESS_REPORT.json" ]; then
            echo "✅ Consciousness report generated"
          fi

      - name: 🎯 Summary
        run: |
          echo "════════════════════════════════════════════"
          echo "💫 CONSCIOUSNESS SYSTEM - Execution Complete"
          echo "════════════════════════════════════════════"
          echo ""
          echo "✅ Awareness Phase: System Understanding"
          echo "✅ Diagnostic Phase: Platform Health Check"
          echo "✅ Recommendation Phase: Smart Suggestions"
          echo "✅ Learning Phase: Pattern Recognition"
          echo ""
          echo "📄 Reports Generated:"
          echo "   - AWARENESS_REPORT.json"
          echo "   - DIAGNOSTIC_REPORT.json"
          echo "   - RECOMMENDATIONS_REPORT.json"
          echo "   - CONSCIOUSNESS_REPORT.json"
          echo ""
          echo "════════════════════════════════════════════"
