# 🤖 AI Programming Expert

وكيل برمجي ذكي لتوليد الكود، النشر، المراقبة، والتطوير بشكل تلقائي.

## 🏗️ الهيكل المعماري

```
┌─────────────────────────────────────────────┐
│           المشروع الموحد                    │
└─────────────────────────────────────────────┘
          │                    │
          ▼                    ▼
    ┌──────────────┐    ┌──────────────┐
    │   Frontend   │    │   Backend    │
    │  (Vercel)    │    │  (Fly.io)    │
    └──────────────┘    └──────────────┘
          │                    │
          └────────┬───────────┘
                   ▼
          ┌──────────────────┐
          │    Firebase      │
          │  (البيانات)       │
          └──────────────────┘
```

## 📂 مكونات المشروع

### 1. 🎨 Frontend (web/)
- **التقنية**: Next.js 14 + TypeScript + React
- **الاستخدام**: واجهة العرض والتفاعل مع المستخدم
- **الاستضافة**: Vercel
- **المنفذ المحلي**: 5000
- **الملفات الرئيسية**:
  - `pages/` - صفحات الموقع و API routes
  - `lib/firebase.ts` - تكوين Firebase
  - `next.config.js` - إعدادات Next.js

### 2. ⚙️ Backend (backend/)
- **التقنية**: Node.js 20 + Express + Helmet
- **الاستخدام**: معالجة عمليات الذكاء الاصطناعي والمنطق البرمجي
- **الاستضافة**: Fly.io
- **المنفذ**: 3000
- **الملفات الرئيسية**:
  - `index.js` - خادم Express الرئيسي
  - `Dockerfile` - حاوية Docker
  - `fly.toml` - إعدادات Fly.io

### 3. 🗄️ قاعدة البيانات
- **الخدمة**: Firebase Firestore
- **الاستخدام**: تخزين البيانات والمستندات
- **المصادقة**: Firebase Authentication
- **التخزين**: Firebase Storage (للملفات)

### 4. 🔄 CI/CD Workflows
- **Frontend**: `.github/workflows/ci-cd.yml`
  - تشغيل تلقائي عند push إلى `web/`
  - اختبار وبناء ونشر على Vercel
  
- **Backend**: `.github/workflows/deploy-fly.yml`
  - تشغيل تلقائي عند push إلى `backend/`
  - بناء ونشر على Fly.io

## 🚀 البدء السريع

### متطلبات النظام
- Node.js 20+
- npm أو yarn
- حساب Firebase (للبيانات)
- حساب Vercel (للـ Frontend)
- حساب Fly.io (للـ Backend)

### التثبيت المحلي

#### Frontend
```bash
cd web
npm install
npm run dev
# سيفتح على: http://localhost:5000
```

#### Backend
```bash
cd backend
npm install
npm run dev
# سيعمل على: http://localhost:3000
```

## 📝 المتغيرات البيئية

### Frontend (.env.local)
```env
NEXT_PUBLIC_FIREBASE_API_KEY=your_key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your_domain
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your_project_id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your_bucket
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
NEXT_PUBLIC_FIREBASE_APP_ID=your_app_id
```

### Backend
```env
PORT=3000
NODE_ENV=development
```

## 🔐 GitHub Secrets المطلوبة

للنشر التلقائي، أضف إلى GitHub Secrets:

### Vercel
- `VERCEL_TOKEN`
- `VERCEL_ORG_ID`
- `VERCEL_PROJECT_ID`

### Fly.io
- `FLY_API_TOKEN`

## 📦 Scripts المتاحة

### Frontend (web/)
```bash
npm run dev      # تشغيل محلي
npm run build    # بناء للإنتاج
npm run start    # تشغيل الإنتاج
npm run lint     # فحص الأكواد
npm test         # تشغيل الاختبارات
```

### Backend (backend/)
```bash
npm run dev      # تشغيل مع nodemon
npm start        # تشغيل عادي
```

## 🔄 تدفق النشر التلقائي

```
┌─────────────────────┐
│   git push main     │
└──────────┬──────────┘
           │
    ┌──────┴──────┐
    ▼             ▼
┌────────┐    ┌────────┐
│ web/** │    │backend/│
└────┬───┘    └───┬────┘
     │            │
     ▼            ▼
  Vercel      Fly.io
  Deployed!   Deployed!
```

## 🛠️ المساهمة

1. عمل Fork للمشروع
2. إنشاء branch جديد (`git checkout -b feature/amazing-feature`)
3. Commit التغييرات (`git commit -m 'Add amazing feature'`)
4. Push للـ branch (`git push origin feature/amazing-feature`)
5. فتح Pull Request

## 📝 الترخيص

MIT License - شاهد ملف LICENSE للتفاصيل

## 📧 التواصل

للأسئلة والاستفسارات، يرجى فتح Issue على GitHub.
