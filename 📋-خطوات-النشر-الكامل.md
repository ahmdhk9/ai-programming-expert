# 📋 خطوات النشر الكامل على Fly.io

## 1️⃣ المتطلبات (تحقق من أنها موجودة):

```bash
✅ fly.toml - موجود
✅ Dockerfile - موجود
✅ backend/package.json - موجود
✅ backend/index.js - موجود
```

## 2️⃣ النشر على Fly.io:

### الطريقة 1: استخدام flyctl CLI (من Terminal)

```bash
# تثبيت flyctl
brew install flyctl

# التحقق من تسجيل الدخول
fly auth login

# النشر
cd backend
fly deploy --remote-only

# فحص الحالة
fly status
fly logs
```

### الطريقة 2: استخدام CI/CD (GitHub Actions)

```yaml
# .github/workflows/deploy-fly.yml يفعّل تلقائياً عند:
- Push إلى backend/
- Push إلى .github/workflows/deploy-fly.yml

المتطلبات:
✅ FLY_API_TOKEN في GitHub Secrets
✅ Workflow معدّة بشكل صحيح
```

## 3️⃣ الخطوات:

### أولاً: تفعيل GitHub Secrets

1. اذهب إلى: https://github.com/ahmdhk9/ai-programming-expert/settings/secrets/actions

2. أضف الـ secrets:
   - Name: `FLY_API_TOKEN`
   - Value: (الـ token من Fly.io)

3. أضف الـ secrets الـ Vercel:
   - VERCEL_TOKEN
   - VERCEL_ORG_ID
   - VERCEL_PROJECT_ID

### ثانياً: اضغط Push

```bash
# أي تعديل في backend/ أو workflows/ سيفعّل النشر تلقائياً
git push origin main
```

### ثالثاً: فحص النشر

```
Dashboard: https://fly.io/apps/agent-backend-ahmd1
Status: Watch deployments
Logs: Monitor the build
```

## 4️⃣ النتيجة:

```
✅ Backend ينشر على: https://agent-backend-ahmd1.fly.dev
✅ Health Check: https://agent-backend-ahmd1.fly.dev/health
✅ API يعمل: curl https://agent-backend-ahmd1.fly.dev
```

## 5️⃣ فحص القدرات التشغيلية:

- ✅ CI/CD Workflow معدّة
- ✅ Fly.io App موجود
- ✅ Dockerfile جاهز
- ✅ fly.toml معدّة
- ✅ Backend Server معدّ

## 🚀 للبدء الآن:

اختر إحدى الطريقتين:

### خيار 1: Manual Deploy
```bash
fly auth login
cd backend
fly deploy
```

### خيار 2: Automatic CI/CD
```bash
# ضع FLY_API_TOKEN في GitHub Secrets
# اضغط push إلى backend/
# GitHub Actions تتولى الباقي
```
